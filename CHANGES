Please log any changes you make to this document in the form:
<function name>: <description of changes> (<name>).

Ex: init_board: board is now a global variable, so this function was changed to take no parameters (Sean).

checkers.h, pretty much everything else: changed functions to take a board as a variable. Allows for local modification of copies of boards. Probably easier for the AI. (Sean).
